# Copy to .env.local for local dev. On Vercel, set these in Project Settings → Environment Variables.

# football-data.org free token — https://www.football-data.org/client/register
FOOTBALL_DATA_TOKEN=

# Optional — TheSportsDB second-source fallback (cross-checks scores). Defaults
# work out of the box: free/test key "3", FIFA World Cup league 4429, season 2026.
THESPORTSDB_KEY=
THESPORTSDB_LEAGUE_ID=
THESPORTSDB_SEASON=

# Slack incoming webhook → your #world-cup channel
SLACK_WEBHOOK_URL=

# Random secret protecting the cron endpoint (openssl rand -hex 24)
CRON_SECRET=

# Admin login password
ADMIN_PASSWORD=

# Public base URL of the deployed app (e.g. https://world-cup-sweepstake.vercel.app)
NEXT_PUBLIC_APP_URL=

# Neon Postgres — added automatically by the Vercel Neon integration.
# DATABASE_URL = pooled, DATABASE_URL_UNPOOLED = direct.
DATABASE_URL=
DATABASE_URL_UNPOOLED=
